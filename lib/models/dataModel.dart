//simple data model used to hold and store values to build widget
class Metrics {
  final String label;
  final int weight;
  final String unit;
  final String assetName;

  Metrics(this.label, this.weight, this.unit, this.assetName);
}
